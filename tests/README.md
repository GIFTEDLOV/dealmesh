# Tests

Stage 0 has no contract or frontend implementation, so there are no
executable tests yet. Stage 1 must add direct-mode tests for deterministic
logic and mocked non-deterministic tests for parser and consensus behavior.
Studio/integration tests must be added only when the contract exists and must
not be represented as green before they run.
